import { UPDATE_DATA_STATE } from '../constants/actionTypes';

export const get = (uid) => (dispatch, getState) => {

};

export const set = (uid, data) => (dispatch, getState) => {

};

export const remove = (uid) => (dispatch, getState) => {

};
